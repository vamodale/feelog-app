import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    ButtonBack: {
        marginTop: 48,
        marginLeft: 32,
        borderRadius: 24,
        width: 48,
        height: 48,
        borderColor: '#FFFFFF',
        borderWidth: 1
    },

    Icon: {
        marginTop: 11,
        marginLeft: 11,
    }
})